# dvbcss-services example

This example will demonstrate how a simple server application can be put
together using the `dvbcss-protocols`. The client uses `bbcat-orchestration` and
its `dvbcss-sync-adapter` to build an audio player synchronised between multiple
browsers.
