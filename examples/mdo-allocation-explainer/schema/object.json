{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Object",
  "description": "Object properties as used by the allocation algorithm and output by the authoring tools",
  "type": "object",
  "properties": {
    "objectId": {
      "description": "Unique identifier for this object",
      "type": "string",
      "required": true
    },
    "objectName": {
      "description": "Human-readable name for this object",
      "type": "string",
      "required": true
    },
    "objectLabels": {
      "description": "Custom labels applied to the object for grouping",
      "type": "array",
      "required": true,
      "items": {
        "type": "string"
      },
      "uniqueItems": true
    },
    "objectPriority": {
      "description": "The object with the lowest priority value is processed first. Behaviours can only refer to objects of lower priority.",
      "type": "number",
      "required": true
    },
    "objectPan": {
      "description": "Stereo panning position, -1 for full left, +1 for full right",
      "type": "number",
      "minimum": -1.0,
      "maximum": 1.0,
      "required": false
    },
    "objectGain": {
      "description": "Gain multiplier",
      "type": "number",
      "minimum": 0,
      "required": false
    },
    "objectImage": {
      "description": "Application-specific reference to an image to display when this object is rendered on a device",
      "type": "string",
      "required": false
    },
    "objectBehaviours": {
      "description": "Behaviours for this object, they can affect placement and rendering",
      "type": "array",
      "required": true,
      "items": {
        "type": "object",
        "properties": {
          "behaviourType": {
            "description": "Type identifier of the behaviour, must be registered with the allocation algorithm",
            "type": "string",
            "required": true
          },
          "behaviourOptions": {
            "descriptions": "Options for the behaviour, specific to the behaviourType, can be any JSON object",
            "type": "object",
            "required": false
          },
          "additionalProperties": false
        }
      }
    }
  }
}
